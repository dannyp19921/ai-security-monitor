spring:
  application:
    name: ai-security-monitor

server:
  port: 8080

# Database config (will be enabled later)
# spring:
#   datasource:
#     url: jdbc:postgresql://localhost:5432/securemonitor
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
